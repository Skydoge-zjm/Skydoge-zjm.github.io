---
interface Props {
  date: Date;
}

const { date } = Astro.props;
const pad = (n: number) => String(n).padStart(2, '0');
const text = `${date.getFullYear()}-${pad(date.getMonth() + 1)}-${pad(date.getDate())}`;
---

<time datetime={date.toISOString()} class="font-mono text-xs tabular-nums">
  {text}
</time>
